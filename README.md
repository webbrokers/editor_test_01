# Loyalty Admin Panel

Панель администратора программы лояльности с визуальным редактором сценариев.

Это проект на [Next.js](https://nextjs.org), созданный с помощью [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Описание проекта

**Loyalty Admin Panel** — это современная административная панель для управления программами лояльности. Основная функция — визуальный редактор сценариев (Canvas), который позволяет создавать сложную логику программы лояльности путем соединения узлов (триггеры, фильтры, действия, рекламные блоки).

### Основные возможности

- 🎨 **Визуальный редактор** — создавайте сценарии с помощью drag-and-drop интерфейса
- 📊 **Управление кампаниями** — создание, редактирование и удаление кампаний
- 💾 **Локальное хранилище** — сохранение конфигураций в localStorage
- ⚡ **Автосохранение** — автоматическое сохранение изменений каждые 2 секунды
- 🎯 **Типы узлов**:
  - Кампания (Campaign)
  - Аудитория (Audience)
  - Фильтр (Filter)
  - Действие (Action)
  - Рекламный блок (Ad Block)

## Начало работы

Сначала установите зависимости:

```bash
npm install
```

Затем запустите сервер разработки:

```bash
npm run dev
# или
yarn dev
# или
pnpm dev
# или
bun dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере, чтобы увидеть результат.

Вы можете начать редактирование страницы, изменяя `app/page.tsx`. Страница автоматически обновляется при редактировании файла.

Этот проект использует [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) для автоматической оптимизации и загрузки [Geist](https://vercel.com/font) — нового семейства шрифтов от Vercel.

## Структура проекта

```
├── app/                    # Страницы приложения (Next.js App Router)
│   ├── campaigns/         # Список кампаний
│   ├── editor/[id]/       # Визуальный редактор сценариев
│   ├── analytics/         # Аналитика (заглушка)
│   └── settings/          # Настройки (заглушка)
├── components/            # React компоненты
│   ├── nodes/            # Компоненты узлов для редактора
│   ├── Header.tsx        # Шапка приложения
│   ├── Sidebar.tsx       # Боковая панель навигации
│   ├── NodeToolbar.tsx   # Панель инструментов узлов
│   └── PropertiesPanel.tsx # Панель свойств узла
├── hooks/                # React хуки
│   └── useEditorStore.ts # Zustand store для редактора
├── types/                # TypeScript типы
│   └── campaign.ts       # Типы для кампаний и узлов
└── utils/                # Утилиты
    └── storage.ts        # Работа с localStorage
```

## Технологии

- **Next.js 15** — React фреймворк
- **React 19** — UI библиотека
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **React Flow** — библиотека для визуального редактора
- **Zustand** — управление состоянием
- **Lucide React** — иконки

## Дополнительная информация

Чтобы узнать больше о Next.js, ознакомьтесь со следующими ресурсами:

- [Документация Next.js](https://nextjs.org/docs) — изучите возможности и API Next.js
- [Изучение Next.js](https://nextjs.org/learn) — интерактивный учебник по Next.js

Вы также можете посетить [GitHub репозиторий Next.js](https://github.com/vercel/next.js) — ваши отзывы и вклад приветствуются!

## Развертывание на Vercel

Самый простой способ развернуть ваше Next.js приложение — использовать [платформу Vercel](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) от создателей Next.js.

Ознакомьтесь с [документацией по развертыванию Next.js](https://nextjs.org/docs/app/building-your-application/deploying) для получения дополнительной информации.
